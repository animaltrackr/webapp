import React from 'react';
import { storiesOf } from '@storybook/react';
import ControlCard from './ControlCard';

storiesOf('ControlCard', module).add('ControlCard', () => (
	<div>
		<ControlCard />
	</div>
));
