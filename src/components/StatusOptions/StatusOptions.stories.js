import React from 'react';
import { storiesOf } from '@storybook/react';
import StatusOptions from './StatusOptions';

storiesOf('StatusOptions', module).add('StatusOptions', () => (
	<div>
		<StatusOptions />
	</div>
));
