import React from 'react';
import { storiesOf } from '@storybook/react';
import SampleContainer from './SampleContainer';

storiesOf('SampleContainer', module).add('SampleContainer', () => (
	<div>
		<SampleContainer />
	</div>
));
